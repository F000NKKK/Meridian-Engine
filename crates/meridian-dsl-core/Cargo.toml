[package]
name = "meridian-dsl-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Generic tag/attribute/children text parser underlying the SDK's extensible scene DSL — no knowledge of any domain tag (Mesh, RigidBody, ...); see meridian-sdk::dsl for that layer."

[dependencies]
meridian-foundation = { workspace = true }
