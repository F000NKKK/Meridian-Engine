[package]
name = "meridian-task-core"
version = "0.3.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Job graph scheduler: worker threads and dependency tracking for parallel frame execution."

[dependencies]
